<!DOCTYPE html>
<html>
<head>
  <title>Valentine</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: pink;
      margin: 0;
    }
    .box {
      background: white;
      padding: 30px;
      border-radius: 15px;
      text-align: center;
      box-shadow: 0 10px 25px rgba(0,0,0,0.2);
      position: relative;
      width: 400px;
    }
    button {
      font-size: 18px;
      padding: 10px 20px;
      margin: 10px;
      cursor: pointer;
      border-radius: 10px;
      border: none;
    }
    #yes { background: lightgreen; }
    #no  { background: lightcoral; position: absolute; }
    img { width: 250px; margin-top: 15px; border-radius: 10px; }
  </style>
</head>
<body>

<div class="box">
  <h1 id="title">Delia, will you be my Valentines?</h1>

  <button id="yes">Yes</button>
  <button id="no">No</button>

  <div id="celebrate" style="display:none;">
    <h2>YAYYY 🎉❤️</h2>
    <img src="kid.jpg">
  </div>
</div>

<script>
  const noBtn = document.getElementById("no");
  const yesBtn = document.getElementById("yes");
  const celebrate = document.getElementById("celebrate");

  document.addEventListener("mousemove", (e) => {
    const rect = noBtn.getBoundingClientRect();
    const dx = rect.left + rect.width/2 - e.clientX;
    const dy = rect.top + rect.height/2 - e.clientY;
    const dist = Math.hypot(dx, dy);

    if (dist < 100) {
      noBtn.style.left = Math.random() * 300 + "px";
      noBtn.style.top  = Math.random() * 150 + "px";
    }
  });

  yesBtn.onclick = () => {
    celebrate.style.display = "block";
  };
</script>

</body>
</html>
